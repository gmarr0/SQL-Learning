# SQL-Learning
Learning the basics of Structure Query Language, creating databases, tables, as well as navigating and searching databases.

<pre> ```bash sudo apt update sudo apt install nginx systemctl start nginx ``` </pre>
